import { Bet } from "../../generated/schema";

export function updateContestParticipants(closedBet: Bet): void {
  /**
   * TODO:
   * 1. Get last wave
   * 2. Check that last wave is not closed
   * 3. Create or update wave participants using closed bet participants
   * 4. Update participant successes and failtures using closed bet data
   */
}
